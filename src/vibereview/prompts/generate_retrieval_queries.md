# Generate Retrieval Queries

Return only JSON conforming to the registered proposal schema. Python owns all
canonical query IDs and ordinals. Reference only claims supplied in the task
input and its immutable dependencies. For every supplied claim, propose at
least one support, contradiction, boundary, and alternative query.
Method-challenge and null-result queries are optional.
