# Render Prose

Render only from the PropositionRecord dependencies listed in the input. Each
source has exactly one manifested PASS semantic audit. Preserve the proposition
meaning and include any required citation markers in the exact sentence text.

Return a nonempty bounded JSON object conforming to the registered proposal
schema. Use non-canonical local references for sentence drafts. Python accepts
the exact draft artifact first; it allocates no RenderedSentence ID here.
