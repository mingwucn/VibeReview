# Hand-off — Milestones B1 + B2: Deterministic Subprocess Runner (r4)

## Summary

Milestones B1 and B2 implemented the subprocess execution contracts, frozen primary-outcome precedence, bounded redacting stream capture, writable-tree and process-group monitoring, race-resistant proposal import, and the deterministic fake agent worker.

- **Commits:**
  - `49357d0` r4a — B1 contracts
  - `5f66d78` r4a — B1 contract tests
  - `a3be05e` r4b — B2 runner + fake worker
  - `f5d490a` r4c — B2 conformance tests
- **Suite status:** 652 passed via `python -m pytest -m "not external_engine" -q` (baseline was 326 at `r3`).

## What was built

### B1 contracts (goal.md §6)

- `runtime/records.py`: `AttemptOutcome` extended with engine failures (`ENGINE_OUTPUT_POLICY_FAILURE`, `ENGINE_RESOURCE_LIMIT_FAILURE`); `FALLBACK_OUTCOMES` = the 7 engine failures; `AttemptFailure` / `AttemptFailureStage` (8 stages) / `ResourceLimitCode` (8 codes); `TaskAttemptRecord.detected_failures`.
- `runtime/subprocess.py`: `SubprocessPolicy`, `deterministic_test_policy()`, `WRITABLE_QUOTA_ROOTS` / `QUOTA_EXEMPT_ROOTS` + `writable_quota_applies()` (output/scratch/home/tmp), `primary_attempt_outcome()` — the frozen §6.6 precedence.
- `runtime/diagnostics.py`: `DiagnosticCapture` (hash covers only the persisted redacted file).
- `runtime/execution_inventory.py`: `ExecutionFileRecord` (unsafe types carry no size/hash).
- `runtime/confinement.py`: `ConfinementLevel`; `allows_real_engine()` is True only for OS_SANDBOX / ENGINE_NATIVE_SANDBOX.
- `runtime/credentials.py`: `CredentialContext` (secrets masked, `exact_redaction_values` excluded from dumps), `EngineCredentialProvider` protocol, `NullCredentialProvider`, `SyntheticCredentialProvider`.
- `runtime/specs.py`: §6.11 object-root proposal-schema preflight (rejects `RootModel`, resolves top-level `$ref`, requires `type: object`).

### B2 runner (goal.md §7)

- `runtime/execution.py`: `ExecutionBackend` protocol, `TemporaryWorkspaceBackend` (TEST_ONLY), `LauncherConfiguration`, `SubprocessEngine` (`AgentEngine`), `SubprocessExecutionResult`, bounded redacting stream capture, SIGTERM→grace→SIGKILL process-group timeout.
- `runtime/output_policy.py`: trusted output-dir fd identity (`O_RDONLY|O_DIRECTORY|O_NOFOLLOW`, st_dev/st_ino recheck before missing-proposal classification), output-tree scan, race-resistant `dir_fd`/`O_NOFOLLOW` proposal import.
- `runtime/resource_limits.py`: writable-tree monitor (scandir/lstat, no follow) with first-breach `ResourceLimitCode` + group kill; process count via `/proc` pgid membership; post-exit final scan.
- `runtime/kernel.py`: fallback integration for non-VALID `execution_report` outcomes.
- `tests/helpers/fake_agent.py`: 32 §7.13 modes, composable via repeated `--mode`.

### Conformance suite

Eleven `tests/runtime/test_subprocess_*.py` modules testing the full boundary under deterministic conditions.
